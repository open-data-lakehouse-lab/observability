# ODL Observability Examples

This directory contains examples of run summaries and other artifacts used by the observability tool.

## Files

- `run-summary.json`: An example of a successful run summary from the orchestration repository.

## Usage

You can use these examples with the CLI:

```bash
odl-observability inspect run --run-summary-path examples/run-summary.json
```
